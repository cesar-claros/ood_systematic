# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
.DS_Store

# Local environments and dataset caches
.venv/
/data/

# Machine-local environment file (HPC paths; the required variables are
# documented in README.md "Environment variables")
.env

# Legacy scripts / configs excluded from the paper repo
*.sh
*.log
*.txt
neural_collapse_vis.py
calibration_example.py
calibration_hyperparameters.py
calibration_plots.py
hpc_scripts/
experiments/
regression_outputs/

# Archived / exploratory code and outputs not used by the paper
archived/

# Downloaded score/metric archives (Google Drive / Zenodo) and their unzipped folders
*.zip
scores/
scores_final/
scores_risk/
scores_risk_resnet18/
scores_calibration/
neural_collapse_metrics/
clip_robustness/
# clip_scores/
clip_scores_openood/

# Analysis outputs (regenerable from the archives; headline numbers are logged
# in ../documentation/NeurIPS_2026_rebuttal_experiments.md)
ood_eval_outputs*/
projection_analysis_outputs/
projection_clique_outputs/
mantel_outputs_papyan/
mantel_partial_outputs/
# nc_csf_predictivity/outputs/

# X8 Pool A caches (features come back from the HPC; synthetic dir is the self-test)
pool_a_features/
x8_pool_a/_synthetic_features/
nc_csf_predictivity/outputs/pool_a/*SYNTHETIC*

# Generated scratch variants (sed copy of stats_eval.py, FPR@95-only metric branch)
stats_eval_fpr_only.py

# Exception to the *.sh rule: the Pool A extraction runner must sync to the HPC
!x8_pool_a/run_pool_a_hpc.sh
!run_new_csfs_pilot.sh
